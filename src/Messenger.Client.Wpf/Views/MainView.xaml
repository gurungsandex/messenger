<UserControl x:Class="Messenger.Client.Wpf.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Conversation list -->
        <DockPanel Grid.Column="0" Background="#F3F3F3">
            <StackPanel DockPanel.Dock="Top" Margin="8">
                <TextBlock Text="Conversations" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
                <TextBox x:Name="UserSearchBox" Text="{Binding UserSearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Search by username or display name" Margin="0,0,0,4" KeyDown="UserSearchBox_KeyDown" />
                <ListBox x:Name="UserSearchResultsBox" ItemsSource="{Binding UserSearchResults}"
                         SelectedItem="{Binding SelectedDirectoryUser}" MaxHeight="120" Margin="0,0,0,4">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="{Binding DisplayName}" FontWeight="Bold" />
                                <Run Text=" (" Foreground="Gray" /><Run Text="{Binding Username}" Foreground="Gray" /><Run Text=")" Foreground="Gray" />
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Content="Start direct chat" Command="{Binding StartDirectConversationCommand}" Padding="4" />
            </StackPanel>

            <ListBox DockPanel.Dock="Top" ItemsSource="{Binding Conversations}" SelectedItem="{Binding SelectedConversation}"
                     BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="8,6">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" />
                            <TextBlock Text="{Binding LastMessageAt}" FontSize="11" Opacity="0.6" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <!-- Chat panel -->
        <DockPanel Grid.Column="1" Margin="12">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="{Binding HeaderTitle}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" />
                <Ellipse Width="10" Height="10" Margin="8,0,0,0" VerticalAlignment="Center" Fill="LimeGreen"
                         Visibility="{Binding PeerOnline, Converter={StaticResource BoolToVisibility}}"
                         ToolTip="Online" />
            </StackPanel>

            <TextBlock DockPanel.Dock="Top" Text="{Binding StatusMessage}" Foreground="DarkOrange" Margin="0,0,0,8" />

            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                <TextBox x:Name="MessageBox" Width="500" Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"
                         KeyDown="MessageBox_KeyDown" />
                <Button Content="Send" Command="{Binding SendMessageCommand}" Margin="8,0,0,0" Padding="8,4" />
                <Button Content="Attach file..." Click="AttachFile_Click" Margin="8,0,0,0" Padding="8,4" />
            </StackPanel>

            <ListBox ItemsSource="{Binding Messages}" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,4">
                            <TextBlock>
                                <Run Text="{Binding SenderDisplayName}" FontWeight="Bold" />
                                <Run Text=" · " Foreground="Gray" />
                                <Run Text="{Binding SentAt}" FontSize="11" Foreground="Gray" />
                            </TextBlock>
                            <TextBlock Text="{Binding Body}" TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Grid>
</UserControl>
