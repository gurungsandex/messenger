<UserControl x:Class="Messenger.Client.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="320">
            <TextBlock Text="Messenger" FontSize="28" FontWeight="Bold" Margin="0,0,0,24" HorizontalAlignment="Center" />

            <TextBlock Text="Username" Margin="0,0,0,4" />
            <TextBox x:Name="UsernameBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Password" Margin="0,0,0,4" />
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,16" KeyDown="PasswordBox_KeyDown" />

            <Button Content="Sign in" Click="SignIn_Click" IsDefault="True" Padding="8" />

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,12,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
